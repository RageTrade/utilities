version: '3.4'
services:
  vault-rebalance:
    build:
      context: .
      target: vault-rebalance
  vault-rebalance-gmx:
    build:
      context: .
      target: vault-rebalance-gmx
  batching-manager:
    build:
      context: .
      target: batching-manager
  keeper-liquidation:
    build:
      context: .
      target: keeper-liquidation
  dn-gmx-batching-manager:
    build:
      context: .
      target: dn-gmx-batching-manager
  dn-rebalance:
    build:
      context: .
      target: dn-rebalance
  jit-keeper:
    build:
      context: .
      target: jit-keeper
